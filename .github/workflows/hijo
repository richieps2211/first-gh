name: Workflow-Hijo

on:
  repository_dispatch:
    types: [trigger-workflow-hijo]

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Paso 1 en Workflow Hijo
        run: echo "Hola desde el paso 1 en Workflow Hijo"
