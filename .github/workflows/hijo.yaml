name: Workflow Hijo

on:
  push:
    branches:
      - main

jobs:
  job2:
    runs-on: ubuntu-latest
    steps:
      - name: Paso 1 en Workflow Hijo
        run: |
          echo "Hola desde el paso 1 en Workflow Hijo"
          echo "BANCO: ${{ github.event.inputs.banco }}"
          echo "PAIS: ${{ github.event.inputs.pais }}"
